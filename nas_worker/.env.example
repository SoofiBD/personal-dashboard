# Store real values only in the root .env.nas.local (ignored by Git).
NAS_HOST=nas.local
NAS_USER=dashboard-service
NAS_PASSWORD=replace-locally
NAS_SHARE=backups
NAS_DOMAIN=
NAS_API_TOKEN=generate-at-least-32-random-characters
