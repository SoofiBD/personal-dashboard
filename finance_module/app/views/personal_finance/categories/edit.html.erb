<header class="page-header">
  <div>
    <p class="eyebrow"><%= t("categories.eyebrow", default: "ORGANİZASYON") %></p>
    <h1><%= t("categories.edit_title", default: "Kategoriyi Düzenle") %></h1>
    <p class="page-subtitle"><%= t("categories.edit_subtitle", default: "Kategori adını, türünü veya renk rozetini değiştirin.") %></p>
  </div>
</header>

<section class="panel form-panel">
  <%= render "form" %>
</section>
