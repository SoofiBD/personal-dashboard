<header class="page-header">
  <div>
    <p class="eyebrow"><%= t("transactions.eyebrow", default: "HESAP HAREKETLERİ") %></p>
    <h1><%= t("transactions.new_title", default: "Yeni İşlem Ekle") %></h1>
    <p class="page-subtitle"><%= t("transactions.new_subtitle", default: "Gelir, gider veya transfer kaydı oluşturun.") %></p>
  </div>
</header>

<section class="panel form-panel">
  <%= render "form" %>
</section>
