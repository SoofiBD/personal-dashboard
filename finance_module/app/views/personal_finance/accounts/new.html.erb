<header class="page-header">
  <div>
    <p class="eyebrow"><%= t("accounts.eyebrow", default: "FİNANSAL TEMELLER") %></p>
    <h1><%= t("accounts.new_title", default: "Yeni Hesap Ekle") %></h1>
    <p class="page-subtitle"><%= t("accounts.new_subtitle", default: "Banka, kredi kartı veya nakit hesabı ekleyin.") %></p>
  </div>
</header>

<section class="panel form-panel">
  <%= render "form" %>
</section>
